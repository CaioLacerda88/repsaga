# Work In Progress

Active branch work. Removed once merged. Empty when no in-flight work exists —
backlog/parked items live in `docs/PROJECT.md` §2.

---

_No in-flight work._ Phase Vitality-3 (strength XP vitality gate + sim recalibration +
anti-cheese invariant fix) merged in #408; migration 00084 pushed to hosted + the
`rpg_strength_vitality_mult` helper smoke-verified live. Condensed in `docs/PROJECT.md`
progress snapshot.

**Queued follow-up (product-owner mapped, not started):** the feel-good UX surfaces that
the per-bp vitality data now unlocks — Charge Ring (readiness HUD), "Your Body Remembers"
comeback beat, Rest Validation debrief, Conditioning Balance radar, weekly Conditioning
Report, opt-in predictive nudge (the last needs Phase-39 ToS sign-off). Phase-39 v1
standouts: Charge Ring + Comeback + Rest Validation (zero new data, no ToS revision,
thesis-pure). All must stay descriptive, never "train-or-lose-it".
