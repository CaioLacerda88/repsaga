# Work In Progress

Active branch work. Removed once merged. Empty when no in-flight work exists —
backlog/parked items live in `docs/PROJECT.md` §2.

---

_No in-flight work._ a11y contrast fixes (#404) + the meta-1.18 lock-truth fix (#403) merged.
Phase 38.9 hardening complete; remaining tracked follow-ups are in `docs/PROJECT.md` §2
(112-site dim-text sweep, active-workout 48dp row-redesign, the `cinematic_tap_hint` flaky test).
