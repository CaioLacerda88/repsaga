# Work In Progress

Active branch work. Each section is removed once the branch is merged. Empty
when no in-flight work exists — backlog and parked items live in
`docs/PROJECT.md` → `## §2 Active Backlog` (single source of truth).

When starting a new task, follow CLAUDE.md → "WIP Tracking": write a checklist
here referencing the relevant `docs/PROJECT.md` phase or backlog entry, check
items off as work lands, and remove the section after the merge condenses
the phase summary in PROJECT.md §4.

---

_No in-flight work._ **Phase 38.9 progress:** Tier 1 (#367/#369/#372) ✅ · Tier 2 T2.1–T2.4
(#374) ✅ · T3.1 finishWorkout decompose (#384) ✅ · T3.2 build() decompose (#385) ✅.
Dependency batch (#386) merged — safe bumps in, supabase_flutter 2.15 deferred (passkeys_web
web-boot crash, see §2 + cluster `supabase-passkeys-web-boot-crash`). **Remaining:** T3.3
(canonical-RPC doc), T3.4 (test hygiene), T2.5/T2.6 (deferred — harder), Tier 0 (launch-readiness,
needs user-provided secrets) — all in `docs/PROJECT.md` §2 → Phase 38.9.
