# Work In Progress

Active branch work. Removed once merged. Empty when no in-flight work exists —
backlog/parked items live in `docs/PROJECT.md` §2.

---

_No in-flight work._ Phase Vitality-2 (per-body-part conditioning delta + cinematic
charge fuse + decaying `vitality_ref_peak`) merged in #406; migration 00083 pushed to
hosted + backfill verified. Condensed in `docs/PROJECT.md` progress snapshot.
