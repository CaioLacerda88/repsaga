{{- if eq .Data.locale "pt" -}}
Redefina sua senha no RepSaga

Recebemos um pedido para redefinir a senha da sua conta. Use o link abaixo para escolher uma nova senha:

{{ .ConfirmationURL }}

Se você não pediu para redefinir a senha, ignore este e-mail — sua senha continua a mesma.
{{/* Add new locale branches HERE — e.g. {{ else if eq .Data.locale "es" }}<es body> */}}
{{- else -}}
Reset your RepSaga password

We received a request to reset your account password. Use the link below to choose a new one:

{{ .ConfirmationURL }}

If you didn't request a password reset, you can safely ignore this email — your password will not change.
{{- end }}
