<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:label="RepSaga"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        tools:replace="android:allowBackup">
        <!-- enableOnBackInvokedCallback is deliberately UNSET (Phase 27 L13.2).
             On Android 14+ with this flag set to "true", Flutter routes back
             gestures via `commitBackGesture` (the predictive-back animation
             path) instead of the legacy `popRoute`. PopScope reliably
             intercepts `popRoute` but the predictive-back integration with
             PopScope inside a GoRouter ShellRoute is incomplete in Flutter
             3.41 — symptom: back press exits the app instead of firing the
             ShellScaffold's `_handlePop`. Logcat shows
             `WindowOnBackDispatcher sendCancelIfRunning` but `_handlePop`
             never runs. Leaving the flag unset forces Android 14+ to use
             the legacy `onBackPressed` path which `FlutterActivity` forwards
             to `popRoute`, where PopScope works correctly. Trade-off: no
             predictive-back gesture preview animation on Android 14+.
             Revisit when Flutter framework's predictive-back integration
             reaches GA inside nested navigators. -->

        <!-- allowBackup=false: prevents Android auto-backup from restoring
             stale Hive box files across uninstall→reinstall cycles. The on-disk
             Hive binary embeds typeIds — if backup-restored data references
             typeIds that the new APK's adapter set has dropped/renumbered,
             the next launch's Hive.openBox throws HiveError and bricks the
             app on the splash screen. RepSaga is fully cloud-backed
             (auth/workouts/PRs in Supabase), so local data is replaceable;
             losing it on uninstall is an acceptable trade.

             tools:replace handles the merge with manifests from any plugin
             (e.g. observability libs) that declares allowBackup=true. -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Deep link for Supabase OAuth callback -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="io.supabase.repsaga"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
